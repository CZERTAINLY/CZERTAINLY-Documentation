import React from 'react';

# JAdES Signer Properties

:::info
See the [Common Properties](../common-properties/basic-properties) for configuration options of the AdES Signer.
:::

The implementation class of the JAdES Signer is:

```
com.czertainly.signserver.module.jades.signer.JAdESSigner
```

It contains the integration with the DSS framework and extends the Worker interface of the SignServer. If you want to use the JAdES Signer, you must specify this property. The JAdES Signer will check the following configuration properties.

export const data = [
    {
        property: "SIGNATURE_PACKAGING",
        description:
            (
                <>
                    Sets the signature packaging. Allowed values are:
                    <ul>
                        <li>ENVELOPING</li>
                        <li>DETACHED</li>
                    </ul>
                </>
            ),
        default:
            (
                <>
                    <span class="badge badge--secondary">NONE</span>
                </>
            ),
        mandatory:
            (
                <>
                    <span class="badge badge--success">YES</span>
                </>
            )
    },
    {
        property: "JWS_SERIALIZATION_TYPE",
        description:
            (
                <>
                    Sets the JWS serialization type. Allowed values are:
                    <ul>
                        <li>COMPACT-SERIALIZATION</li>
                        <li>JSON-SERIALIZATION</li>
                        <li>FLATTENED-JSON-SERIALIZATION</li>
                    </ul>
                </>
            ),
        default:
            (
                <>
                    <span class="badge badge--secondary">NONE</span>
                </>
            ),
        mandatory:
            (
                <>
                    <span class="badge badge--success">YES</span>
                </>
            )
    },
    {
        property: "SIGD_MECHANISM",
        description:
            (
                <>
                    Sets signature mechanism to use for a detached signature packaging. Allowed values are:
                    <ul>
                        <li>NO-SIG-D</li>
                        <li>HTTP-HEADERS</li>
                        <li>OBJECT-ID-BY-URI</li>
                        <li>OBJECT-ID-BY-URI-HASH</li>
                    </ul>
                </>
            ),
        default:
            (
                <>
                    <span class="badge badge--secondary">NONE</span>
                </>
            ),
        mandatory:
            (
                <>
                    Mandatory when the signature packaging = <b>DETACHED</b>
                </>
            )
    },
    {
        property: "BASE64_URL_ENCODED_PAYLOAD",
        description:
            (
                <>
                    Sets if <b>base64Url</b> encoded payload shall be used.
                </>
            ),
        default:
            (
                <>
                    <span class="badge badge--secondary">true</span>
                </>
            ),
        mandatory:
            (
                <>
                    <span class="badge badge--danger">NO</span>
                </>
            )
    },
    {
        property: "BASE64_URL_ENCODED_ETSI_COMPONENTS",
        description:
            (
                <>
                    Sets if the instances of the <b>etsiU</b> header shall appear in their corresponding <b>base64url</b> encoding.
                </>
            ),
        default:
            (
                <>
                    <span class="badge badge--secondary">true</span>
                </>
            ),
        mandatory:
            (
                <>
                    <span class="badge badge--danger">NO</span>
                </>
            )
    }
];

export const GenTable = ({data}) => (
  <React.Fragment>
    {data.map((dat, i) =>{
            return(
                <tr key={i}>
                  <td><b>{dat.property}</b></td>
                  <td>{dat.description}</td>
                  <td>{dat.default}</td>
                  <td>{dat.mandatory}</td>
                </tr>)
         })}
  </React.Fragment>
);

<table>
    <th>Property</th>
    <th>Description</th>
    <th>Default Value</th>
    <th>Mandatory</th>
  <tbody>
    <GenTable data={data}/>
  </tbody>
</table>